Це найважливіша частина твоєї роботи, яка переводить її з розряду "Адміністрування" в розряд **"DevOps та Автоматизація"**.

Коли ми встановимо Ansible і зв'яжемо його з Git, ти отримаєш повний контроль над інфраструктурою.

Ось **4 Реальні Сценарії**, які ти покажеш на захисті, і як саме ми будемо виправляти помилки.

---

### Сценарій 1: "Глобальне оновлення" (Mass Config Push)
**Ситуація:** Директор каже: *"Терміново змінити пароль адміністратора на всіх 7 пристроях і додати новий DNS-сервер 1.1.1.1!"*
**Без Ansible:** Ти заходиш на кожен пристрій руками (7 разів), вводиш команди, помиляєшся, нервуєш. Час: 20 хв.
**З Ansible + Git:**
1.  Ти відкриваєш файл `vars.yml` у себе на комп'ютері.
2.  Змінюєш `admin_password: "NewSecurePass!"`.
3.  Робиш `git push`.
4.  Запускаєш одну команду на `osboxes`.
5.  **Результат:** Через 10 секунд на всіх пристроях змінився пароль.

---

### Сценарій 2: "Машина часу" (Backup & Restore)
**Ситуація:** Ти щось налаштовував на Core-Switch і випадково видалив VLAN-и. Мережа впала.
**Без Ansible:** Паніка. Ти намагаєшся згадати, які там були IP-адреси.
**З Ansible:**
1.  У тебе налаштований щоденний бекап.
2.  Ти відкриваєш папку `backups/` на Гіті.
3.  Бачиш файл `CORE1_backup_2023-12-03.txt`.
4.  Запускаєш плейбук **"Restore"**.
5.  **Результат:** Конфігурація відновлена до вчорашнього стану за 1 хвилину.

---

### Сценарій 3: "GitOps" (Конфігурація як Код) — 🌟 Це для диплома
Це відповідь на твоє питання *"Чи буде конфігурація на Гіті?"*.
**ТАК.**

Ми зробимо так, що **Git буде "Джерелом Правди" (Source of Truth)**.
1.  Уся твоя мережа описана в текстових файлах (YAML) у репозиторії.
2.  Якщо ти хочеш додати новий VLAN 40, ти **не лізеш** у консоль Cisco.
3.  Ти додаєш рядок у файл на GitHub:
    ```yaml
    vlans:
      - id: 40
        name: NEW_DEPT
    ```
4.  Ansible бачить це і сам створює VLAN на потрібних світчах.

---

### Сценарій 4: "Лікар Мережі" (Drift Correction / Виправлення помилок)
Це відповідь на *"чи зможемо ми виправляти помилки"*.

**Ситуація:**
Молодший адмін (або хакер) зайшов на `R1` руками і змінив DNS на `6.6.6.6` (неправильний). Це називається **Configuration Drift** (Дрейф конфігурації).

**Як це виправляє Ansible:**
1.  Ти запускаєш Ansible (або він сам запускається за розкладом).
2.  Ansible порівнює:
    *   В Гіті написано: DNS = `8.8.8.8`.
    *   На роутері зараз: DNS = `6.6.6.6`.
3.  Ansible каже: *"Невідповідність! Виправляю!"*.
4.  Він видаляє `6.6.6.6` і повертає `8.8.8.8`.

**Висновок:** Твоя мережа сама себе "лікує", повертаючись до еталонного стану, прописаного в Гіті.

---

### Як це виглядає технічно (Твій Workflow)

Ось схема, яку ти можеш намалювати в дипломі:

1.  **Ти (Адмін)** сидиш за своїм ноутбуком. Пишеш код.
2.  `git push` $\rightarrow$ **GitHub** (Зберігання історії змін).
3.  `git pull` $\rightarrow$ **osboxes** (Забирає свіжий код).
4.  **Ansible** (на `osboxes`) $\rightarrow$ **SSH** $\rightarrow$ **Cisco Devices** (Застосовує зміни).

---

### Що ми можемо налаштувати через код?

Для Cisco IOSv у GNS3 ми можемо автоматизувати 99% задач:
*   Створення користувачів.
*   Налаштування інтерфейсів (IP, description).
*   Налаштування OSPF / EIGRP / BGP.
*   Створення VLAN.
*   Зміна Banner MOTD (повідомлення при вході).
*   Налаштування SNMP (щоб Zabbix працював).

### Мій план для тебе

Зараз, поки качається і оновлюється `osboxes`, твоя задача:
1.  Дочекатися встановлення Ansible.
2.  Переконатися, що SSH працює (з тим файлом конфігу, що ми зробили).
3.  Я дам тобі **перший простий скрипт**, який просто збере всі конфіги з усіх пристроїв і покладе їх у папочку.

Це буде твоя перша перемога в автоматизації. Готовий?
